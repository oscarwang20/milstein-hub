export type DBMember = {
  netid: string;
  fname: string;
  lname: string;
  email: string;
  major: string;
  year: string;
};

export type DBEvent = {
  id: string;
  name: string;
  location: string;
  description: string;
};
